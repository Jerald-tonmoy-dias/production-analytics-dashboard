# No application secrets are required in v1.
# RSC fetch to this app's Route Handlers needs an absolute origin on the server.
# Browser calls use a same-origin relative URL.
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# On Vercel, `VERCEL_URL` is used when NEXT_PUBLIC_APP_URL is unset.
